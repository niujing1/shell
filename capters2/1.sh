#!/usr/bin/env bash

#ls列出信息 -F显示文件类型 目录后边有 / 管道后边有| 可执行文件后边是*
ls -F #注意大小写敏感,不能把-F写成-f

#ls -l显示出详细信息
ls -l

#ls -lh将文件的大小以更加友好的方式显示 k G B

#ls -la 会将隐藏文件显示
ls -la

#ls -lt 将文件按最后访问时间排序

ls -ltr #将排序的结果反转


